import fs from 'fs'
import path from 'path'

export default function Home() {
  return (
    <main>
      {/* Your content is in index.html — 
          move the <body> content here as JSX,
          or simply serve index.html as a static file */}
    </main>
  )
}